



def modifiedString (phrase:str)-> str:
    """ check the type and replace it with a small letter,separates the word then return modified string"""

    for i in range ("phrase"):
      
      if phrase==phrase.__str__ and phrase == phrase.upper:
       phrase = phrase + " "
     
    
    

    return phrase.lower



modifiedString("HelloWord")